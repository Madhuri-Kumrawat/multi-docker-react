sudo: required
services:
  - docker

before_install:
  - docker build -t mady/multi-docker-react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run  -e CI=true mady/multi-docker-react-test npm run test
